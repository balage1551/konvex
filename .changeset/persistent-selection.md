---
"@balage1551/konvex-editable-line": minor
---

Add a `persistentSelection` option to `EditableLine` (also a live reactive ref). When `true`, the point selection is kept while the line is deactivated; when `false` (default), deselecting the line clears its point selection.
